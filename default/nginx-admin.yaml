apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-admin
  namespace: default
  labels:
    app: nginx-admin
    managed-by: portainer-idp
  annotations:
    portainer-idp/git-target-id: '1'
    portainer-idp/git-branch: main
    portainer-idp/git-path: default/nginx-admin.yaml
    portainer-idp/edge-stack: 'true'
    portainer-idp/edge-group-ids: '1'
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nginx-admin
  template:
    metadata:
      labels:
        app: nginx-admin
        managed-by: portainer-idp
    spec:
      containers:
        - name: app
          image: nginx
          imagePullPolicy: Always

apiVersion: apps/v1
kind: Deployment
metadata:
  name: uitest-review
  namespace: web
spec:
  replicas: 1
  selector: {matchLabels: {app: uitest-review}}
  template:
    metadata: {labels: {app: uitest-review}}
    spec: {containers: [{name: app, image: "nginx:alpine"}]}

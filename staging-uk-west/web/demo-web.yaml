apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-web
  namespace: web
  labels:
    app: demo-web
    managed-by: portainer-idp
  annotations:
    portainer-idp/git-target-id: '2'
    portainer-idp/git-branch: main
    portainer-idp/git-path: staging-uk-west/web/demo-web.yaml
    portainer-idp/edge-stack: 'true'
    portainer-idp/edge-group-ids: '8'
spec:
  replicas: 1
  selector:
    matchLabels:
      app: demo-web
  template:
    metadata:
      labels:
        app: demo-web
        managed-by: portainer-idp
    spec:
      containers:
        - name: app
          image: nginx:alpine
          imagePullPolicy: Always

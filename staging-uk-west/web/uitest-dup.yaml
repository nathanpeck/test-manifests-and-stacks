apiVersion: apps/v1
kind: Deployment
metadata:
  name: uitest-dup
  namespace: web
spec:
  replicas: 1
  selector: {matchLabels: {app: uitest-dup}}
  template:
    metadata: {labels: {app: uitest-dup}}
    spec: {containers: [{name: app, image: "nginx:alpine"}]}
